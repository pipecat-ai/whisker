- Added a bus messages panel: every `BusMessage` the runner sees is captured and streamed to the UI. Messages can be filtered by category (lifecycle / frame / job / other) and by message type via a searchable dropdown. The most recent 200 events are also kept in a server-side ring buffer and replayed to a fresh client on connect.
