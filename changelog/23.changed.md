- ⚠️ Adopted Pipecat's `task` → `worker` rename throughout Whisker. `WhiskerServer.create_observer()` (and `WhiskerSink.create_observer()`) now take a `PipelineWorker`, observers receive `worker_name` instead of `task_name`, and the wire protocol's per-frame `task_id` field became `worker_id`. Setup files should define `setup_pipeline_worker(worker)` (not `setup_pipeline_task(task)`) and register the sink with `runner.add_workers(server)` (not `runner.spawn(server)`).
